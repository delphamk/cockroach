# Regression test for #38551.
build
SELECT * FROM ROWS FROM (count(json_each('[]')))
----
error (42803): count(): aggregate functions are not allowed in FROM

# error (0A000): count(): set-returning functions are not allowed in aggregate
